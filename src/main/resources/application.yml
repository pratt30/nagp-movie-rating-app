server:
  port: 8080

spring:

  datasource:
    url: jdbc:mysql://mysql-service:3306/ratingsdb
    username: movieuser
    password: moviepass123

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

management:

  endpoints:
    web:
      exposure:
        include: health,info,metrics

  endpoint:
    health:
      probes:
        enabled: true